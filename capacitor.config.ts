import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'supabase-ionic-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
